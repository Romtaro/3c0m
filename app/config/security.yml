# To get started with security, check out the documentation:
# http://symfony.com/doc/current/security.html
security:

    # http://symfony.com/doc/current/security.html#b-configuring-how-users-are-loaded
    providers:
         mysql_provider:
            entity:
                class: ENTSiteBundle:Contact
                property: pseudo
      #  in_memory:
      #      memory:
      #          users:
      #              draden:
      #                    password: draden
      #                    roles: 'ROLE_USER'
      #              admin:
      #                    password: admin
      #                    roles: 'ROLE_USER, ROLE_ADMIN'

    firewalls:
        # disables authentication for assets and the profiler, adapt it according to your needs
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        default:
            anonymous: ~
            http_basic: ~

        main:
            anonymous: ~

            # activate different ways to authenticate

            # http://symfony.com/doc/current/security.html#a-configuring-how-your-users-will-authenticat
        default:
            anonymous: ~
            form_login:
              login_path: login
              check_path: login
              require_previous_session: false
              default_target_path: /membre
            provider: mysql_provider
            logout:
                path: /logout
                target: /
    encoders:
        Symfony\Component\Security\Core\User\User: plaintext
#TROUVER UN PUTIN DE MOYEN POUR SE DELOG' DU profiler

    access_control:
        #- { path: ^/admin, role: ROLE_ADMIN }
        #- { path: ^/res$, role: ROLE_USER }
        #- { path: ^/faq, role: ROLE_USER }
      #  - { path: ^/support, role: ROLE_USER }
      #  - { path: ^/membre, role: ROLE_USER }
      #  - { path: ^/gcompte, role: ROLE_ADMIN }
      #  - { path: ^/galert, role: ROLE_ADMIN }
